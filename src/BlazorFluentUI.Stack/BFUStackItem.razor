@namespace BlazorFluentUI
@inherits BFUComponentBase

@if(ChildContent != null)
{
    <div class=@($"mediumFont ms-StackItem {ClassName}") style=@($"{GetStyles()}{Style}")>
        @ChildContent
    </div>
}