@namespace BlazorFluentUI
@inherits BFUComponentBase
@using Microsoft.AspNetCore.Components.Web
<BFUGlobalCS Component="this" CreateGlobalCss=@(()=>CreateGlobalCss(Theme)) />

@if (_needMeasurement)
{
    <div id="@Id" class=@($"{GlobalClassNames["root"]} {ClassName}") title="@Title" style=@($"white-space: nowrap; {Style}") @ref=RootElementReference>
        @Title
    </div>
}
else if (ShouldTruncate && !string.IsNullOrWhiteSpace(TruncatedTitleFirstPiece) && !string.IsNullOrWhiteSpace(TruncatedTitleSecondPiece))
{
<div id="@Id" class=@($"{GlobalClassNames["root"]} {ClassName}") title="@Title" @ref=RootElementReference style=@Style>
    @TruncatedTitleFirstPiece
    &hellip;
    @TruncatedTitleSecondPiece
</div>
}
else
{
    <div id="@Id" class=@($"{GlobalClassNames["root"]} {ClassName}") title="@Title" @ref=RootElementReference style=@Style>
        @Title
    </div> 
}