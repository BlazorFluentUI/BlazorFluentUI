@namespace FluentUI

@foreach (var portalPair in portalFragments)
{
    <LayerPortal @key=@portalPair.Id Id=@portalPair.Id @ref=@portals[portalPair.Id!] >
        @portalPair.Fragment
    </LayerPortal> 
}
