<LayerHost Style="display:flex; flex-direction: row;width:100vw;height:100vh;overflow:hidden;">
    <Router AppAssembly="typeof(Program).Assembly" AdditionalAssemblies="new[] { typeof(MainLayout).Assembly }">
        <Found Context="routeData">
            <ResponsiveCascader>
                <FabricTheme Theme="@(new DefaultTheme())">
                    <RouteView RouteData="routeData" DefaultLayout="@typeof(MainLayout)" />
                </FabricTheme>
            </ResponsiveCascader>
        </Found>
        <NotFound>
            <p>Page not found.</p>
        </NotFound>
    </Router>
</LayerHost>
